import { projectSchema, productSchema, authorSchema, journalPostSchema } from '../../sanity/schema'

export const schemaTypes = [
  projectSchema,
  productSchema,
  authorSchema,
  journalPostSchema,
]
